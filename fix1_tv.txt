                <div className="dustyTV">
                  {/* 3x3 grid assembly */}
                  <div className="tv-grid">
                    <img src={tvTL} className="tv-c" alt="" />
                    <img src={tvTop} className="tv-h" alt="" />
                    <img src={tvTR} className="tv-c" alt="" />

                    <img src={tvLeft} className="tv-v" alt="" />
                    <div className="tv-screen" key={`detail-${game.app_name}`}>
                      <h2 className="dustyDetailTitle">
                        {game.overrides?.title || game.title}
                      </h2>
                      {info.developer && (
                        <p className="dustyDetailDev">{info.developer}</p>
                      )}
                      {info.extra?.about?.shortDescription && (
                        <p className="dustyDetailDesc">
                          {info.extra.about.shortDescription}
                        </p>
                      )}
                      <div className="tv-divider" />
                      <div className="tv-meta">
                        <span className="tv-meta-label">
                          {t('game.status', 'Status')}
                        </span>
                        <span className={`tv-meta-val ${game.is_installed ? 'ok' : ''}`}>
                          {game.is_installed
                            ? t('status.installed', 'Installed')
                            : t('game.notInstalled', 'Not installed')}
                        </span>
                      </div>
                      <button
                        className="tv-play-btn"
                        onClick={() => activateGame(game)}
                        disabled={!idle}
                      >
                        {game.is_installed
                          ? `▶ ${t('label.playing.start', 'Play')}`
                          : `↓ ${t('game.install', 'Install')}`}
                      </button>
                    </div>
                    <img src={tvRight} className="tv-v" alt="" />

                    <img src={tvBL} className="tv-c" alt="" />
                    <img src={tvBot} className="tv-h" alt="" />
                    <img src={tvBR} className="tv-c" alt="" />
                  </div>
                </div>